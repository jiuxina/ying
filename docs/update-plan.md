# 萤 · 小部件个性化更新计划

本文档是 Android 桌面小部件个性化功能的目标清单与开发路线，整理自 2026-08-08 的功能脑暴。计划按阶段推进，每个阶段尽量保持可独立发版，避免长期只有一个半成品分支。

## 标记规则

- `- [ ]`：未开始
- `- [ ]（进行中）`：正在开发，尚未完成
- `- [x]`：已完成，并在条目末尾标注完成版本或日期

## 开发顺序原则

1. 先做底座：数据协议、准点刷新、配置面板是后续所有功能的地基，先打通可减少返工。
2. 视觉先行：壁纸贴纸、取色、主题皮肤的用户感知最强，适合作为第一波发版内容。
3. 内容与交互并行：滚动列表、自定义文案、分区点击彼此独立，可拆成两个小版本。
4. 实用增强与双端一致：在 Android 功能稳定后统一补齐 iOS WidgetKit 对等实现。
5. 趣味主题包放最后：不阻塞主线发版，作为彩蛋版本输出。

## 阶段总览

| 阶段 | 范围 | 优先级 | 状态 | 建议版本 |
| --- | --- | --- | --- | --- |
| 阶段 0 | 数据协议与刷新底座 | P0 | [x] 已完成（2026-08-08） | 2.1.0 |
| 阶段 1 | 视觉与壁纸个性化 | P1 | [x] 已完成（2026-08-08） | 2.1.0 |
| 阶段 2 | 内容个性化 | P1/P2 | [x] 已完成（2026-08-08） | 2.2.0 |
| 阶段 3 | 交互增强 | P1/P2 | [x] 已完成（2026-08-08） | 2.2.0 |
| 阶段 4 | 实用增强与双端一致 | P1/P2 | [x] 已完成（2026-08-09） | 2.3.0 |
| 阶段 5 | 趣味主题包 | P2 | [ ] 未开始 | 2.3.0 |

## 阶段 0：数据协议与刷新底座

后续视觉、内容、交互全部依赖小部件数据层，先把协议、刷新与配置面板打通。

详细计划、验收标准与验证命令见 [docs/phase-0-plan.md](phase-0-plan.md)。

- [x] P0 扩展小部件数据协议：为 `WidgetEvent` 与 `HomeWidgetPreferences` 预留背景图、主题、单位文案、Emoji、进度、精确时间等字段，读取全部使用 `opt*` 兼容旧数据（2026-08-08，v2 协议）
- [x] P0 午夜准点刷新：用 `AlarmManager.setWindow` 在每天 00:00 后的 5 分钟窗口内触发一次小部件更新，避免 30 分钟轮询造成天数显示滞后（2026-08-08）
- [x] P0 应用内配置面板扩展：按「样式 / 内容」重构分区，预览区实时同步新配置；背景与交互等各自功能落地时再显示（2026-08-08）
- [x] P1 配置数据迁移与降级：旧小部件缺少新字段时保持现有显示效果，不强制用户重新配置（2026-08-08）

手工验收：MuMu 模拟器（x86_64 / Android 12）确认旧小部件视觉与现状一致、跨零点 5 分钟窗口内刷新、改系统时间 / 时区后立即刷新，且无需精确闹钟权限（2026-08-08）。

注意事项：

- RemoteViews 只支持有限 View 操作，动画、手势、自定义 View 受限，后续交互方案需要提前规避。
- 数据协议必须向后兼容，新字段一律 `optString` / `optBoolean` / `optLong` 读取并回退默认值。
- 系统对 `updatePeriodMillis` 有最小 30 分钟限制；准点刷新评估 `WorkManager` 一次性任务与精确闹钟权限，注意厂商电池优化和自启动限制。
- 配置写入后调用 `HomeWidget.updateWidget` 或发送刷新广播，确保小部件即时反映新配置。

## 阶段 1：视觉与壁纸个性化

用户感知最强的第一波功能，优先实现透明贴纸、壁纸取色与相册背景图。

详细计划、验收标准与验证命令见 [docs/phase-1-plan.md](phase-1-plan.md)。

- [x] P1 透明贴纸模式：去掉卡片底色，数字与标题浮在壁纸上，用描边或阴影保证可读性（2026-08-08）
- [x] P1 壁纸取色自动配色：应用侧读取 `WallpaperManager` 主色与深色，生成文字对比色后写回小部件配置（2026-08-08）
- [x] P1 相册背景图模式：使用系统 Photo Picker 选图，压缩缓存后写入小部件配置（2026-08-08）
- [x] P1 材质主题：玻璃、拍立得、霓虹、像素、极简五套预设（2026-08-08）
- [x] P1 节日皮肤：圣诞、生日、新年、中秋等按日期自动切换（2026-08-08）
- [ ] P2 深色与动态色跟随：Android 12+ 支持 Material You 动态取色，跟随系统深浅色
- [ ] P2 天气联动背景：可选接入天气源，按晴、雨、雪更换背景或副文案

注意事项：

- 壁纸取色在应用侧完成，小部件只读取计算好的颜色值，避免在 RemoteViews 中做复杂计算。
- 相册图片必须压缩缓存后再提供给小部件，防止大图解码导致内存问题；Android 13+ 用 Photo Picker 可避免存储权限。
- 字体与主题素材优先打包进资源，RemoteViews 不支持运行时下载字体。
- 节日皮肤按本地日期判断，避免时区边界导致提前或延迟切换。

## 阶段 2：内容个性化

让同一个事件在不同小部件上呈现不同信息密度与文案风格。

详细计划、验收标准与验证命令见 [docs/phase-2-plan.md](phase-2-plan.md)。

已实现，MuMu 模拟器手工验收通过（2026-08-08）。

- [x] P1 多事件滚动列表：用 `RemoteViewsService` 实现可滚动事件列表（2026-08-08）
- [x] P1 自定义单位文案：支持「还有、只剩、距离、已经、约 X 周」等预设（2026-08-08）
- [x] P1 事件 Emoji / 图标显示（2026-08-08）
- [x] P1 精确到时分秒显示（可开关）（2026-08-08）
- [x] P2 农历与星期显示（2026-08-08）
- [x] P2 进度百分比 / 进度环：从创建日到目标日的完成进度（2026-08-08）
- [x] P2 神秘模式：只显示蜡烛、进度或「快到了」，不显示具体数字（2026-08-08）
- [x] P2 每日一句 / 备注轮播：多条备注按天切换（2026-08-08）
- [x] P2 数字字体选择：手写、等宽、像素等打包资源字体（2026-08-08）

手工验收：MuMu 模拟器（x86_64 / Android 12）确认各内容开关与预览即时同步、单事件卡片与滚动列表渲染正常、旧小部件保持原显示，且无「无法加载微件」提示（2026-08-08）。

注意事项：

- 滚动列表依赖 `RemoteViewsService` 与 collection view；数据变化时需要调用 `notifyAppWidgetViewDataChanged` 刷新。
- 时分秒显示会带来更高的刷新频率，默认关闭，并评估功耗影响。
- 农历换算建议复用成熟算法或本地库，避免自造日历逻辑引入日期错误。
- 轮播文案要保证旧数据没有备注时自动隐藏，不占小部件空间。

## 阶段 3：交互增强

在小部件有限交互能力内，把点击目标拆得更细，并补齐快捷入口。

已实现（2026-08-08）。

- [x] P1 分区点击：标题区打开详情，完成按钮快速完成，日期区复制卡片（2026-08-08）
- [x] P1 直接新建：小部件提供「+」按钮，直接打开新建事件表单（2026-08-08）
- [x] P1 快速完成与 6 秒撤销：完成状态写回小部件，窗口内可恢复（2026-08-08）
- [x] P1 跨日翻牌动效：天数变化时用短时两帧切换模拟翻牌（2026-08-08）
- [ ] P2 长按复制分享卡片：复制「距离 XXX 还有 N 天」
- [ ] P2 连点彩蛋：连续点击数字触发隐藏文案或小动画
- [ ] P2 滚动列表交互：在阶段 2 列表上支持上下滚动浏览

注意事项：

- RemoteViews 不支持手势，滑动切换等交互需要借助按钮、状态位或 collection view 实现，不能依赖触摸事件。
- 每个可点击区域都要有明确的无障碍语义与 contentDescription。
- 快速完成与撤销需要与 App 内撤销逻辑共用同一持久化模型，避免小部件和 App 状态不一致。
- 点击意图统一使用 `PendingIntent.FLAG_IMMUTABLE`，避免重复创建与安全告警。

## 阶段 4：实用增强与双端一致

在 Android 个性化功能稳定后，补齐实用场景与 iOS 对等能力。

详细计划、验收标准与验证命令见 [docs/phase-4-plan.md](phase-4-plan.md)，平台调研结论见
[docs/phase-4-platform-research.md](phase-4-platform-research.md)。

- [x] P1 最后 N 天高亮：小于 7 / 3 / 1 天自动切换强调色与文案（2026-08-09）
- [x] P2 双小部件联动：总览小部件显示最近事件，详情小部件专注单个事件，每实例独立索引（2026-08-09）
- [x] P2 锁屏 / 负一屏形态调研：Android 无系统级锁屏 / 负一屏挂载 API，输出结论后不实现（2026-08-09）
- [x] P2 iOS 对等实现：临近高亮与单事件专注小部件已在 WidgetKit 落地（2026-08-09）

手工验收：MuMu 模拟器（x86_64 / Android 12）已安装 2.3.0 调试构建并启动；
临近高亮与单事件小部件桌面验收通过（2026-08-09，用户复核）。

注意事项：

- 最后 N 天高亮与节日皮肤都依赖准点刷新，必须先完成阶段 0 的刷新底座。
- 双小部件联动需要为每个 widget 实例保存独立配置，注意多实例之间的数据隔离。
- iOS WidgetKit 的交互和刷新机制与 Android 不同，优先保证核心视觉一致，交互差异单独评估。

## 阶段 5：趣味主题包

不阻塞主线发版，作为彩蛋版本输出。

- [ ] P2 神秘信封：点击掀开才看到事件内容
- [ ] P2 时间胶囊：目标日当天小部件变成恭喜卡片
- [ ] P2 复古 CRT 扫描线样式
- [ ] P2 霓虹灯牌数字
- [ ] P2 像素血条式剩余进度
- [ ] P2 镜像 / 旋转整活样式

注意事项：

- 趣味主题复用阶段 1 的主题架构，不新增独立数据模型。
- 每个主题都要通过对比度与 200% 字号回归，不能牺牲可读性。

## 全局注意事项

- RemoteViews 硬限制：不支持手势、复杂动画与自定义 View；滚动用 `RemoteViewsService`，动画用状态位或短时两帧切换。
- 权限与后台：相册优先走 Photo Picker；准点刷新评估精确闹钟权限，并接受厂商后台限制的现实。
- 图片资源：大图先压缩缓存，路径持久化到 `HomeWidgetPreferences`，避免每次刷新重新解码。
- 兼容性：小部件 JSON 保持版本号，新增字段全部向后兼容，旧数据回退默认值。
- 无障碍：所有按钮保留 `contentDescription`，滑动操作保留按钮替代入口，通过 200% 文字缩放回归。
- 测试：每个阶段完成时运行 `flutter analyze` 与 `flutter test`；小部件逻辑用 Android 单元测试或模拟器截图验证，覆盖深色 / 浅色、大字、小 / 中尺寸。
- 发版：阶段 0-1 合并为 2.1.0，阶段 2-3 合并为 2.2.0，阶段 4-5 视完成情况进入 2.3.0；每个已勾选目标写入对应 release notes。

## 完成标准

单个目标勾选 `[x]` 前必须满足：

- [x] 功能在真机或模拟器桌面实际生效，应用内预览同步展示（2026-08-08）
- [x] 旧小部件缺少新配置时表现与现状一致（2026-08-08）
- [x] 无障碍语义与 200% 字号回归通过（2026-08-08）
- [x] `flutter analyze` 与 `flutter test` 通过（2026-08-08）
- [x] README 功能特性与 release notes 同步更新（2026-08-08）
